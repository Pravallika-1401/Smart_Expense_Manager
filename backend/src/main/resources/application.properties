spring.application.name=backend

# Aiven MySQL Database
spring.datasource.url=jdbc:mysql://mysql-17dea75-expensemanager2025.a.aivencloud.com:12534/defaultdb?ssl-mode=REQUIRED&sslCA=classpath:ca.pem

spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT
jwt.secret=${JWT_SECRET:myExpenseManagerSecretKey2024VeryLongAndSecure}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Port
server.port=${PORT:8080}